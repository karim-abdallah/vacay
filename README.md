# vacay
Excellence, innovation, climate pledge.

This repository contains the front-end for the Vacay application, as well as the back-end for shared aplications (jarvis-hr).

# Local development
Make sure to use the `vacay` .npmrc file and use the default npm registry.

# To run
```bash
yarn install
yarn start
```
